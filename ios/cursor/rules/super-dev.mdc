---
description: Super-dev standard rules for iOS development
globs: **/*.{swift,h,m,plist,json}
---

# Super-dev iOS Cursor Rules

## 1) 协作规范
1. 注释、分析、回复统一使用中文，技术术语可保留英文。
2. 优先复用现有能力：`MCP` > `skills` > 脚本。
3. 遵循最小必要读取原则。

## 2) 提交与本地化
1. 提交前必须确保本地化已同步（参考 `sync-add-ios-loc` 技能）。
2. 提交信息简洁，不超过 20 字。

## 3) 技能包
项目技能存储在 `.agents/skills` 目录中。

