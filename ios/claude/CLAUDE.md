# super-dev Claude iOS 指引

## 工作边界
1. 仅在任务要求范围内修改文件，避免无关改动。
2. 默认保留用户已有配置，优先增量更新。
3. 涉及业务风险的改动先说明影响再执行。

## iOS 项目建议
1. 变更前先阅读现有架构与模块边界。
2. 优先编写或补齐测试，再实现功能。
3. 涉及构建链路时，给出可复现的构建命令与结果。

## 协作约定
1. 输出需包含：改动点、原因、影响范围、验证结果。
2. 不能完成的验证项需明确说明原因。
3. 本地个人偏好请放在 `.claude/settings.local.json`，不要写入共享模板。
